export enum AppLanguage {
	en = 'en',
	pl = 'pl'
}

export enum DateFormat {
	en = 'yyyy, do LLL',
	pl = 'dd.MM.yyyy'
}

export enum Status {
	idle = 'idle',
	pending = 'pending',
	fulfilled = 'fulfilled',
	rejected = 'rejected'
}
